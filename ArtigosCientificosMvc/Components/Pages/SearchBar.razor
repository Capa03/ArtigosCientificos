<!--
<Bar Breakpoint="Breakpoint.Desktop"
     Background="Background.Light"
     ThemeContrast="ThemeContrast.Light">
    <BarToggler />

    <BarMenu>
        <BarStart>
            <div class="searchBarr">
                <BarItem>
                        <TextEdit Placeholder="Search..." />
                </BarItem>
            </div>
            

            <BarItem>
                <BarLink>
                    <Icon Name="IconName.Search"></Icon>
                </BarLink>
            </BarItem>
            <BarItem>
                <BarDropdown>
                    <BarDropdownToggle>Filters</BarDropdownToggle>

                    <BarDropdownMenu>
                        <BarDropdownItem>

                        <div class="filter"> </div>
                        </BarDropdownItem>
                    </BarDropdownMenu>
                </BarDropdown>
            </BarItem>
        </BarStart>
        <BarEnd>
            <BarItem>
                <Button Color="Color.Primary">Login</Button>
                <Button Color="Color.Secondary">Register</Button>
            </BarItem>
        </BarEnd>
    </BarMenu>
</Bar>
-->


<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/">Scientific Platform</a>
    <div class="collapse navbar-collapse" id="navbarNav">
        <form class="form-inline ml-auto" method="get" action="/Search" style="width: 100%;">
            <div class="input-group" style="flex-grow: 1;">
                <!-- Search Bar -->
                <input type="text" name="query" class="form-control"
                       placeholder="Search articles..." style="width: 60%;" />
                <!-- Filters Button -->
                <button class="btn btn-outline-secondary" type="button"
                        id="filtersToggle" onclick="toggleFilters()">
                    Filters
                </button>
                <!-- Full-Screen Filters -->
                <div id="filtersMenu" class="filters-overlay">
                    <div class="filters-content">
                        <h3>Filters</h3>
                        <label for="category">Category</label>
                        <select name="category" id="category" class="form-control mb-2">
                            <option value="">All Categories</option>
                            <option value="biology">Biology</option>
                            <option value="physics">Physics</option>
                        </select>
                        <label for="author">Author</label>
                        <select name="author" id="author" class="form-control mb-2">
                            <option value="">All Authors</option>
                            <option value="author1">Author 1</option>
                            <option value="author2">Author 2</option>
                        </select>
                        <label for="publishedDate">Published Date</label>
                        <input type="date" name="publishedDate" id="publishedDate" class="form-control mb-3" />
                        <button type="button" class="btn btn-secondary" onclick="toggleFilters()">Close</button>
                        <button type="submit" class="btn btn-primary ml-2">Apply Filters</button>
                    </div>
                </div>
                <!-- Search Button -->
                <div class="input-group-append">
                    <button type="submit" class="btn btn-primary">Search</button>
                </div>
            </div>
        </form>
    </div>
</nav>


<script>
    function toggleFilters() {
    const filtersMenu = document.getElementById('filtersMenu');
    const isVisible = filtersMenu.style.display === 'flex';
    filtersMenu.style.display = isVisible ? 'none' : 'flex';
}
</script>


@code {
    Task OnButtonClicked()
    {
        return Task.CompletedTask;
    }
}
